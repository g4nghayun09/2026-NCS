## 이름 : 강하윤

# JavaScript 기초 문법



# 자바스크립트 환경 설정

1. 마이크로소프트에서 제공하는 개발 IDE인 비주얼 스튜디오 공식사이트트(https://code.visualstudio.com)에서 다운로드 후 설치 (바탕화면 만들기, path추가)
2. Node.js의 공식 사이트(https://nodejs.org)에 접속해서 Node.js을 다운로드 받아서 설치
3. vs 실행 후 terminal 에서
   - node -v 실행(버전 확인)

   - npm -v 실행(버전 확인)

   -에러 발생시??

4. vs 에 유용한 도구 설치

- html css suppport, HTML snippets(geyao가 제공=> 상황에 따라 선택), JavaScript (ES6) code snippets, ESLint, Prettier- Code formatter(이후 두 가지 설정), live server

- Prettier-Code formatter 설정 : 좌측 하단에 톱니바퀴 모양 선택 -> setting -> default formatter -> Prettier-Code formatter 로 설정 , format on save 를 체크

---

# 1. 자바스크립트란

- **동적인 웹 사이트 제작을 위해 사용**됨

1. 역사 : 1990년대 넷스케이프의 브랜던 아이크(Brenden Eich)가 Mocha라는 이름으로 출시하고, 라이브스크립트로 개발하였다가 최종적으로 자바스크립트가 됨. 그러나 브라우져(넷스케이프, 익스플로러....)에 따라 상이한 스크립트가 나오기 시작하면서 개발이 쉽지 않았으며(if 구문의 남발...).... 스크립트의 표준화에 대한 요구가 생기기 시작함.

2. ECMA(European Computer Manufactures Association) 스크립트 : 자바스크립트의 표준작업이 시작된 것으로 1997년 6월 정식으로 채택된 스크립트의 표준. 모던 스크립트는 ES(ECMA script의 약자)6 버전에서 시작한다고 본다.

3. 바닐라 자바스크립트(바닐라 JS) : 어떠한 라이브러리(JQeury..)나 프레임워크(React, Vue, Angular....)를 사용하지 않는 순수 자바스크립트, 가장 빠르고 가벼운 크로스 플랫폼 프레임워크 => 실력 향상을 위한 필수 단계 => 결코 쉽지 않다...=> 그러나 프레임워크나 라이브러리는 모두 바닐라 자바스크립트로 만들어져 있다. => 자신의 실력과 가치를 높이는 강력한 수단이 될 수 있다.

4. 바닐라 자바스크립트 시작하기

- 사이트 : http://vanilla-js.com/
- 시작 : <script src="path/to/vanilla.js"></script>를 html 문서안에 삽입
- 위트 있는 사이트 ^^ : 다운받아도 아무 것도 없다!!!!
- 순수 자바스크립트로 짜면 좋다!!!!

- 최근에는 자바스크립트를 이용하여 웹뿐만 아니라 서버(NodeJS), 데스크톱 프로그램(Electron)도 제작하고 있는 추세
- 최근 [가장 인기있는 언어](http://githut.info/)로 높은 점유율을 차지하고 있음

---

# 2. 자바스크립트 작성 위치

- 외부 파일에서 불러오기 (권장) : ` <script src="./js/01_js_location.js"></script>`
- 파일 안 script 태그에서 직접 사용하기 (수업에서 자주 사용) => `<body>` 중 어디가 가장 좋을까?
- 인라인 자바스크립트 사용
- **CSS의 사용 방식**(외부, style 태그 사용, 인라인 스타일)과 비슷
- 실습 (01_js_location.html)

---

# 3. console 객체의 log 함수

- console에 대한 내장함수를 사용 가능
- 부라우져의 개발자 도구 (f12)를 통해 확인 가능
- 실습 (02_console.html)

---

# 4. 변수 선언자 var, let, const

컴파일 -> 목적프로그램
실행 -> 로딩

- 변수란?
  : 프로그램 실행에 필요한 데이터를 담는 기억공간. 변수는 계속 변한다라는 의미임. 비유를 하자면 그릇에 다가 할 수 있음. 그릇에 뭐를 담느냐에 따라 다름.
  - 장점 : 데이터 타입에 따른 선언(int, float, double.....)이 필요하지 않다.
  - 단점 : 데이터 타입에 관계없는 변수이기 때문에 개발시 오류 발생이 가능.
    var a = prompt(); -> 5(string)
    var b = prompt(); -> 7(string)
    console.log(a+b); -> 12(예상값), 57(현실)
    위와 같은 내용이 대표적인 단점. but 단점 보다는 장점이 많음.

- 변수 선언자 종류 : var (ES6부터)let, const
- 선언 형식 : 선언자 변수명 = 값;
- 변수 작성 규칙(4가지) : 카멜, 스네이크, 파스칼, 헝가리안
  - 카멜 표기법 : 두 가지 이상의 단어가 합쳐질 경우, 두 번째부터 단어의 첫 글자를 대문자로
  - 스네이크 표기법 : 두 개 이상의 단어가 합쳐질 경우 단어 사이에 \_ 삽입.
  - 파스칼 표기법 : 모든 단어의 첫 글자를 대문자로
  - 헝가리안 표기법 : 변수명 맨 앞에 데이터의 타입에 해당하는 영어단어를 추가하고, 나머지는 카멜 표기법을 따른다.
    최근 추세는 카멜 아니면 스네이크임. 하지만 우리는 카멜 표기법을 쓸거임.
- var 선언자의 특징
  - ES6 이전부터 존재함. 굉장히 오래됨.
  - 재선언(동일한 이름으로 재선언이 가능), 재할당(값을 바꾸는 것) 가능.
  - 주로 전역변수에 사용. => 권장x(덮어쓰기 기능 때문에)
- **let** 선언자의 특징
  - ES6 이후 등장
  - 재선언 불가능, 재할당 가능. => 권장
  - 동일한 변수명을 다른 곳에서 사용이 불가능하다. (프로그램 내에 다른 곳임.)
- **const** 선언자의 특징
  - ES6 이후 등장
  - 변하지 않는 상수값 -> 대문자
  - 재선언, 재할당 불가
  - 협업하는 개발자들이 통일해서 사용하는 데이터 선언 등에 사용
    예 : const korea_dateFormat = "YYYY-MM-DD";
- 실습(03_variable_declaration.html)

---

# 5. 기본 데이터 타입

: 선언은 하지 않으나 가지고 있음.

1. 문자열(String) 객체

- 기호
  - "", ''로 할당. 가급적으로 한 가지로 통일하는 것이 좋다.
- 속성 및 함수
  - 문자열데이터, 멤버(속성(파란색)또는 함수(보라색))
- 코딩 규칙
  - 팀별 코딩 규칙에 따라 문자열 "또는'로 지정한다.
- 코딩 중 에러 발생 주의
  - let str1 = "그는 'John'이라고 불립니다";
- 문자열 합치기
- 실습(04_dataType.html)

2. 숫자(Number)형 객체

- 64bit 부동소수점 방식
  - 부호 : 1
  - 지수부 : 11
  - 가수부(소수부) : 52
- 특징
  - 정수, 실수-0 다 포함.
- 속성 및 함수
  - 내장기능(함수)및 속성(프로퍼티) 사용가능
- 64비트 부동소수점 표현의 문제점
  - 자바스크립트는 자바등과 달리 메모리는 64비트 부동소수점 형태로 모든 숫자를 관리 => 문제(약간의 오차)가 발생할 수 있다.
  - 정밀도 오차의 해결방법
- 실습(04_dataType.html)

3. Boolean 객체

- 특징
  - (true, false)
- 실습(04_dataType.html)

4. undefined 객체

- 누가 주는 걸까? : 실행 단계에서 브라우저 엔진이 주는 거임.
- undefined : 에러가 나지 않음 -> 그래서 나오지 않게 하는게 중요!!
- 특징
  - 데이터 타입이자 데이터 값
  - 정의되지 않은, 값을 할당하지 않은 객체
  - 할당하지 않은 개발자의 실수가 있는 경우
  - 실행단계에서 브라우저 엔진이 자동으로 할당하는 값
  - 따라서 개발단계에서 수시로 개발자도구로
  - undifiend가 발생하는지 체크!!
- 실습(04_dataType.html)

5. null 객체
   : 즉 값을 비워주고 싶을 때 사용하는 그런 느낌임.

- 특징
  - 없음을 의미하는 데이터 타입
  - 개발자가 실수가 아니라 일부러 설정하는 경우가 많음.
  - 선언시점에는 어떤 값이나 어떤 데이터 타입이 올지 정해지지 않은 경우
  - Garbage Collection (가비지 컬랙션 : 더 이상 사용하지 않는 데이터를 메모리에서 삭제) (주메모리에 있는 값을 삭제)
    - 주메모리에서 쓰레기 값을 정리해야 빨라짐. 그래서 가비지 컬랙션이 있음.
    - 예시
      let jsonData = "굉장히 큰 데이터";
      // 처리완료
      jsonData = null;
- 실습(04_dataType.html)
- 메모리 (Memory)
  - 주메모리 (RAM) : 16GB, 32GB
  - 보조메모리 : 하드디스크, usb, 외장하드

6. symbol 객체

- 특징

---

# 6. 데이터 타입 - Object

iterable : 순회가 가능한 (순차적으로 돌아다닌다.) --> Dictionary(딕셔너리)유사

파이썬에는 키에 숫자 가능.
- { 키(key) : 값(value) }
        // - 키 값은 유일하다. (즉 하나 밖에 없음)
        // - 키 값은 문자열이다.
        // - 값 : 문자열, 숫자, boolean, [](배열), {}(객체)......

json과 객체의 차이점 : json은 

- 정의
  - : 관련잇는 데이터를 하나로 담을 수 있는 객체
- 사용법
  - : {}를 사용
- 특징
- : 관리가 매우 쉽다.
   - 파이썬의 dictionary(딕셔너리)와 비슷함.
   1. 관련있는 정보를 하나로 묶어서 관리하고 싶은 경우 사용
      : 사람-이름, 전화번호, 이메일주소, 집 주소

1. 관련 있는 정보를 하나로 묶어서 관리하고 싶은 경우 사용

2. 키(key : name, phone, email 등 ....) 와 값(value)를 쌍으로 갖는다.

3. 객체의 값에 접근하는 방법(.연산자 사용) : 이름.키

4. 객체의 값을 변경시

5. 객체에 새로운 키와 값을 추가

6. object 의 특정 키에 접근하는 방법

- 실습(05_Object.html)

---

# 7. 데이터 타입 - Array

1. 배열의 정의
  - 하나 이상의 데이터를 담을 수 있다. 
  - 데이터의 순서가 있다. --> 데이터에 접근하고 수정, 편집이 가능하다.

  - []를 이용해서 할당
  - 어떤 데이터 타입도 담을 수 있다.
  - 어떤 데이터 타입도 함께 담을 수 있다. let mix = [1, "", true, {}, []]; --> 다가능함.
2. 배열의 순서
  - 순번 : 인덱스 (index)
  - 인덱스는 0번부터 1씩 증가한다.
  - 인덱스를 이용하여 데이터에 접근 및 수정이 가능하다.

3. 배열의 활용
  - 다양한 내장함수(push(), find(), concat().......)를 사용할 수 있다.
  - object와 함께 활용이 가능하다. (배열 안데 object가 있는 형태)

- 실습(06_Array.html)

---

# 8. 64비트 부동소수점
(그냥넘어감)
1. 에러를 발생시켜 보자.
2. 에러의 발생을 미연에 방지하려면?
3. 자바스크립트에서 사용하는 가장 큰 수와 작은 수는?

- 실습

---

# 9. 연산자

1. 할당 연산자

- 특징
  : 오른쪽 피연산자의 값을 왼쪽 변수에 할당.
- 종류
  : =, 복합 할당 연산자 (+=, -=, *=, /=, %=)
- 실습(08_operator.html)

2. 비교 연산자

- 특징 
  : 값(타입) 비교.
- 종류 
  : (==, ===, !=, !==, >, >=, <, <=)
- 값과 타입의 비교 
  : ===

- 실습(08_operator.html)

3. 산술 연산자
산술 연산자
  - 단항 : ++ -- + -
  - 이항 : + - * % /
- 특징
  : 증가, 감소, 단항 부정 (-), 숫자화 (+)
- 종류
  : +, -, *, /, %, **, ++, --
- 실습(08_operator.html)

3-1. 단항 부정 연산자

- 특징
  : 부호가 바뀜.
- 실습(08_operator.html)

3-2. 숫자화 연산자

- 특징
  : 숫자가 아닌 문자/boolean 값을 숫자로 변환하는 시도
- 실습(08_operator.html)

4. 논리 연산자

- 특징
  : 조건을 여러가지 만족시키는지 알아보는 연산자
- 종류
  : &&(둘 다 true일 경우 true), ||(둘 중 하나만 true이면 true)
- 실습(08_operator.html)

5. 문자열 관련 연산자

- 특징
  : 합치기
- 종류
  : +
- 실습(08_operator.html)

6. 조건(삼항) 연산자

- 특징  
  : 조건 ? true일 때 할당할 값 : false 일 때 할당할 값
    중첩해서 사용이 가능하다.
- 종류
- 실습(08_operator.html)

---

# 10. 조건문

한줄 코딩보다는 가독성이 좋은 코드를 작성하는 것이 중요함.
조건문은 일관성있게 하는 것이 가독성이 좋음. 큰것부터 작은거 처럼 작성해야함.

- 특징
  - 가장 중요한 구문 중 하나이다.
  - 프로그램 = 조건문의 집합
  - if : 만일 ~라면...
  - 작성 시 순서가 효율성에 영향을 미친다.

1. 조건문 if

- if
- if else
- if else if else if .... else
- 항상 false 인 값 :
- 실습(09_if.html)

2. 조건문 switch

- 특징
  - 상수값만 비교..
  - 실무에서 거의 사용하지 않는다. 
  - 가끔 좋아하는 사람이 있음. (덕후같은 사람)
- 장점
  : if문을 사용하면 모든 조건을 비교하면서 내려오지만 switch문의 경우 값을 비교해서 바로 찾아간다. 즉 성능 향상 장점이 있다 => 컴퓨터의 성능이 워낙 좋으므로 크게 신경 쓰지 않는다.
- 단점
  : 오히려 코드가 길어지는 단점이 있다.
- 실습(10_switch.html)

- 참고: **Date**객체

---

# 11. 반복문

- 특징
  - 특정 코드 블록을 정해진 횟수만큼 반복해서 실행하게 하는 문장

1. for loop

- 특징
  - 배열에서 가장 많이 사용
- 동작 원리
  - for(statement1 : statement2 : statement3);
    - statement1 : 코드 블록을 실행하기 전에 무조건 한 번 실행하는 초기화 문장
    - statement2 : 코드 블록을 실행시킬 조건절
    - statement3 : 코드 블록 실행한 후 실행되는 횟수 증가 코드.
    1 => 2 => 참 코드실행 => 3 => 2 => 코드실행 => 3 => 2 ...... => 조건 거짓 => 종료
  - 조건절이 참인 동안 실행할 코드. 거짓이면 빠져나옴. 
- 실습(11_for_loop.html)

- for 과 break
  - break를 만나면 반복문 블록을 중단하고 블록을 빠져나온다.
- for 과 continue
  - continue를 만나면 그 단계는 진행하지 않고 다음 step으로 넘아간다.

1-1. for ~ in

- 특징
  - 배열, object에서 많이 사용
  - 그러나 배열에서 인덱스가 중요한 것은 아니므로 주로 객체에서 사용한다.
- 실습(12_for_in.html)

1-2. for ~ of

- 특징
- 실습(13_for_of.html)

1-3. for ~ each

- 특징
- 실습(14_for_each.html)

2.  while loop

- 특징
- 실습(15_while.html)

3.  do - while

- 특징
- 실습

4. 반복문의 활용

---

# 12. 함수

여기에 시험문제 3문제가 나옴. 암기해야함.

자바스크립트의 메모리
  - 브라우저 엔진이 메모리에 올려놓는 순서
    1. 먼저 선언함수 -> 로딩 (function을 확인) 그래서 function을 맨위 꼭대기로 올려버림.
    -> 그래서 함수를 아무대나 작성해도 된다는 거임.
    -> 호출 후 선언 가능
    2. var -> 로딩
    그 다음 순서대로 실행됨. 

- 정의
  - 반복적인 기능, 재사용해야 할 코드들의 모임
  - 재사용 가능한 구조로 만든 코드들의 집합 => 중복 작업을 피하고 효율성을 높일 수 있다.
- 특징
  - 앞으로 변경이 가능한 값을 가진 경우 적합
  - 팀프젝의 경우, common.js파일을 만들어 사용하면 수정과 적용에 매우 우수한 효율성을 보인다.
  - common.js파일의 주석(comment)을 생성해서 사용권장
  - 주석을 생성하는 방법?

4가지의 종류
  1. 함수 선언 방식
  2. 함수 표현 방식
  3. 화살표 함수 - 많이 사용
  4. 생성자 함수 - 많이 사용x

1. 함수 선언식 방식

- 선언
  - function 함수명 (매개변수1, 매개변수2......) { .... 코드블록 .... } - return문 없어도 됨.
    매개변수1, 매개변수2..... - 함수 내에서만 사용가능.
- 사용
  - 선언하고 호출하여 실행한다.
- 특징
  - 함수 선언식 방식은 선언하는 **위치에 관계없이** 브라우저 엔진이 가장 먼저 메모리에 로딩한다. 
    따라서 쾨드 순서장 선언 전에 호출하여도 무방하다.
- 실습(16_function.html)
- common.js 파일의 활용 및 장점

2. 함수 표현식 방식

- 선언
- 사용
- 유의할 사항
- 실습(16_function.html)

3. Function 생성자 함수

- 선언
- 사용
- 언제 유용하게 사용할까?
- 실습(16_function.html)

4. 화살표 함수

5. 함수 자체가 파라미터로 전달되는 함수

- forEach() 실습

# 13. 시험관련 - 강하윤
- 시험은 이 노트를 보고 공부 하면됨. 

